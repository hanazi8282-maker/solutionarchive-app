Headline KPI readout: label + big coloured % + progress bar.

```jsx
<StatGauge label="전사 종합 달성률" value={38} caption="이번 주" />
<StatGauge value={82} size="md" />
```

The number and bar colour follow the achievement rule automatically. Sizes `sm`/`md`/`lg`.

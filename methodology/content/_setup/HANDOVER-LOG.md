# 인수인계 — LOG (미해결·사고·확인사항)

> **성격**: **append-only.** 항목을 지우지 않는다. 해소되면 `[해소 YYYY-MM-DD]` 마킹만 한다.
> **짝 문서**: `HANDOVER-SEED.md` — 설계·구조·인프라 (분기 1회 갱신)
> **관련**: `_setup/infra-decisions.md` — 인프라 제안 반복 추적
> **갱신**: 세션 종료 시마다

---

## 0. 판독 규칙

| 신호 | 의미 |
|---|---|
| 30일 넘은 미해결이 **5개 이상** | ⚠️ 우선순위 문제가 아니라 **계획이 틀린 것** |
| 같은 항목이 **3세션 연속** 미해결 | `infra-decisions.md`로 승격 검토 |
| 미해결에 **사고가 부착됨** | 반복 횟수와 무관하게 **강제 착수** |
| 해소 항목이 **재발** | 근본 원인 미해결. 새 엔트리가 아니라 원 엔트리에 재발 행 추가 |

**나이 계산**: `발생일` 기준. 오늘 날짜와 비교한다.

---

## 1. 즉시 확인 사항

### 1-1. ⚠️ 다른 채팅에서 나간 틀린 지시가 실행됐는지

채팅 「프로젝트 설정 로드 완료, 다음 액션 선택 필요」(URL: `claude.ai/chat/93abf3b3-2164-4d14-8748-295531c632ab`, 2026-09-03 14:36)에서 Claude가 다음과 같이 판정했다.

> **"05-provenance.md 완전 부재 → 영구 결번으로 인정, README에 '부재' 명시하고 진행"**
> Claude Code 지시: **"05-provenance.md는 재작성하지 않는다. README.md 파일 지도 테이블에 '부재 — 원문 대조 검증(B-3) 수행 불가' 한 줄만 추가한다."**

**이 판정은 틀렸다.** 파일은 `/home/claude/work/final/05-provenance.md`에 11,196바이트로 온전히 존재했고 2026-09-04 세션에서 복구·전달 완료했다.

**확인할 것**
- `methodology/content/solfa/README.md`의 파일 지도 표에 **"부재 — 원문 대조 검증(B-3) 수행 불가"** 줄이 들어갔는가? 있으면 삭제해야 한다.
- Claude Code가 커밋 `7e15df1`에서 "검증기의 `solfa: provenance=False` 전제를 되돌렸다"고 보고했으므로 검증기는 정리됐을 가능성이 높으나, **README는 별개 파일**이다.

### 1-2. 같은 채팅의 미해결 질문 3건

Claude가 물었으나 답을 못 받은 것:

| # | 질문 |
|---|---|
| a | 검증 스크립트의 UTF-8 `MISSING`이 "BOM 없음"인지 "인코딩 판정 자체 실패"인지 — 해당 함수 코드 확인 필요 |
| b | `sync-solfa-skill.sh`(bash)와 "scripts/는 전부 .mjs"라는 보고가 모순 — 실제로 둘 다 있는지, 하나로 통일됐는지 |
| c | `feedback-loop-design.md`가 이 아카이브 관련 문서인지, 완전 별개인지 |

### 1-3. 네임스페이스 수정본이 리포에 반영됐는지

2026-09-04 세션 마지막에 **코드 네임스페이스 3층 충돌**을 해소한 전량 교체본을 전달했다(§11 참조). 리포 반영 여부 확인 필요. 반영 안 됐으면 §15의 프롬프트 A를 Claude Code에 투입.

### 1-4. 미커밋 잔재

Claude Code 마지막 보고 기준 미정리 항목:
- `cross-summary.md` — 미커밋
- `setup/` 디렉터리 — untracked (`_setup/`으로 이름 변경 필요)
- 리포 루트의 `solfa/` — untracked, 삭제 대상 (정본은 `methodology/content/solfa/`)
- `feedback-loop-design.md` 삭제 미스테이징

---


---

## 2. 미해결 항목 (append-only)

> **컬럼 규칙**: `발생일`은 그 항목이 처음 인지된 날. `상태`는 `미해결` / `진행중` / `[해소 YYYY-MM-DD]` / `[기각 YYYY-MM-DD]`.
> **해소된 항목도 지우지 않는다.** 재발 시 원 엔트리에 행을 추가한다.

### 2-1. 리포 반영 대기

| # | 항목 | 발생일 | 나이 | 상태 | 조치자 |
|---|---|---|---|---|---|
| L-05 | 네임스페이스 수정본 전량 교체 (solfa 6 + pdp 6 + 루트 5 + `_setup` 6) | 2026-09-04 | 0d | 미해결 | Claude Code |
| L-06 | `verify-additions.py`를 `verify-methodology-archive.py`에 병합 (화이트리스트 유지 필수) | 2026-09-04 | 0d | 미해결 | Claude Code |
| L-07 | `sync-content-skill.sh` 재실행 → references 8개 동기화 | 2026-09-04 | 0d | 미해결 | Claude Code |
| L-08 | SKILL.md 게이트 참조 `P-` → `G-` 갱신 | 2026-09-04 | 0d | 미해결 | Claude Code |
| L-09 | 미커밋 잔재: `cross-summary.md` / `setup/`→`_setup/` / 루트 `solfa/` 삭제 / `feedback-loop-design.md` | 2026-09-03 | 1d | 미해결 | Claude Code |
| L-26 | `지문` 필드 반영본 커밋 (04×2, 00-gate, 검증기) | 2026-09-04 | 0d | 미해결 | Claude Code |
| L-27 | `infra-decisions.md` 커밋 | 2026-09-04 | 0d | 미해결 | Claude Code |
| L-28 | `HANDOVER-SEED.md` / `HANDOVER-LOG.md` 커밋 | 2026-09-04 | 0d | 미해결 | Claude Code |

### 2-2. 미결정 (남헌 판단 필요)

| # | 항목 | 발생일 | 나이 | 상태 | 제안 |
|---|---|---|---|---|---|
| L-10 | `_setup` vs `setup` 폴더명 | 2026-09-04 | 0d | 미해결 | `_setup` 권장 |
| L-11 | 네임스페이스 규약표 위치 (02 말미 vs 00 상단) | 2026-09-04 | 0d | 미해결 | 현재 02 말미 |
| L-12 | `eval-set.md` 문항/정답 물리 분리 | 2026-09-04 | 0d | **진행중** | INF-04, 반복 3회 도달. 프롬프트 B-CC STEP 1 |
| L-13 | 판정 로그 `지문` 필드 | 2026-09-04 | 0d | **[해소 2026-09-04]** | 04×2 + 00-gate + 검증기 반영 완료 |
| L-14 | `MANIFEST.txt` (md5 드리프트 검출) | 2026-09-03 | 1d | **미해결 ⚠️ 사고 2건** | **INF-01 강제 착수 대상** |
| L-15 | `infra-decisions.md` | 2026-09-04 | 0d | **[해소 2026-09-04]** | 생성 완료. INF-01~04 등재 |
| L-16 | `cross-summary.md` 자동 생성 스크립트 | 2026-09-04 | 0d | 미해결 | INF-03 |
| L-17 | pdp `_source/` 원문 파일 컨벤션 (단일 vs 챕터별) | 2026-09-03 | 1d | 미해결 | 출처 마커가 여기 걸림 |
| L-18 | Threads "끝까지 읽은 비율" 접근 가능 여부 | 2026-09-03 | 1d | 미해결 | 불가 시 저장률로 대체 |
| L-29 | HANDOVER SEED/LOG 분리 | 2026-09-04 | 0d | **[해소 2026-09-04]** | INF-02 완료 |

### 2-3. 미착수 작업

| # | 항목 | 발생일 | 나이 | 상태 |
|---|---|---|---|---|
| L-19 | **평가 세트 30문항 응시 — 남헌 직접** (프롬프트 B) | 2026-09-04 | 0d | 미해결 (⚠️ D 시작 전 필수) |
| L-30 | **평가 하니스 구축 — 스킬 품질 측정** (프롬프트 B-CC) | 2026-09-04 | 0d | **진행중** | INF-05 |
| L-20 | 프로젝트 지식 v2 교체 (7파일 ≈60KB) | 2026-09-04 | 0d | 미해결 |
| L-21 | 역방향 5건 (프롬프트 C) | 2026-09-04 | 0d | 미해결 |
| L-22 | 첫 정방향 1회전 (프롬프트 D) | 2026-09-04 | 0d | 미해결 |
| L-23 | hermes-loop Notion 2차 확장 (프롬프트 F) | 2026-09-03 | 1d | 미해결 |
| L-24 | 스킬 실제 발동 확인 (새 세션 3종 테스트) | 2026-09-03 | 1d | 미해결 |
| L-25 | 세 번째 방법론 자료 처리 (프롬프트 E) | 2026-09-04 | 0d | 미해결 |

### 2-4. 제품 트랙 (범위 밖, 기록만)

| 항목 | 발생일 | 상태 |
|---|---|---|
| Stage 4 앵글 결과 UI | 2026-09-02 | 미해결 |
| Threads 콘텐츠 루프 생성 로직 재설계 (첫 3개 초안 품질 미달) | 2026-09-02 | 미해결 |
| Track C 소스·카테고리 확장 (Google Play 승인 대기) | 2026-09-02 | 미해결 |
| 멱등키 설계 구멍 | 2026-09-02 | 미해결 |
| 원문 폐기 배치 스위치(`REVIEW_PURGE_APPLY`) | 2026-09-02 | 미해결 |
| YouTube·네이버 API 키 발급 + 러너 인증 헤더 코드 | 2026-09-02 | 미해결 |
| Phase 1: 소구점 발굴 로직 실제 제품 1개 수동 실행 | 2026-09-02 | 미해결 |

### 2-5. 집계

| 구간 | 건수 |
|---|---|
| 총 미해결 | **23** |
| 30일 초과 | **0** ✅ |
| 사고 부착 | **1** (L-14 → INF-01) |
| 이번 세션 해소 | **3** (L-13, L-15, L-29) |
| 진행중 | **3** (L-12, L-19→보류, L-30) |

> 30일 초과가 0이므로 현재 계획은 유효하다. **L-14가 유일한 강제 착수 대상.**

---

## 3. 사고 이력 (append-only) ★ 반드시 읽을 것

### 3-1. 발생한 사고 5건

| # | 사고 | 정체 | 발견 경로 |
|---|---|---|---|
| 1 | UTF-8 바이트 2개 유실 (`C가`→`C\xb0\x80`, `C를`→`Cꥼ`) | 인코딩 손상 | 디코딩 실패로 자동 발견 |
| 2 | **solfa 자리에 pdp 사본** (6파일 md5 동일) | 이름은 맞는데 **내용이 다름** | Claude Code가 다른 작업 중 우연히 |
| 3 | **02-gate.md 구버전 커밋** (G-R 누락) | 이름도 내용도 있는데 **버전이 다름** | Claude Code 보고와 프로젝트 지식 대조 |
| 4 | **게이트 P-10 vs 규칙 P-10** | **같은 이름이 두 뜻** | 보고받은 다른 건 확인 중 발견 |
| 5 | README가 없는 `prompts/` 참조 | 이름은 있는데 **대상이 없음** | Claude Code 보고 |
| 6 | **"05-provenance 영구 결번" 오판** | **"찾지 못함"을 "없음"으로 접음** | `/summary` 실행 중 과거 대화 검색으로 |

**전부 "이름과 실체의 불일치"이고, 전부 `§n-m` 댕글링 검사를 통과했다.** 그 검사는 문서 안의 논리적 참조만 봤기 때문이다.

### 3-2. 도출된 규약

1. **§7.1 재확인** — 사고 6이 정확히 이 규약 위반이었다. 파일이 안 보인다고 없다고 판정하지 말 것. **작업 디렉터리·트랜스크립트·과거 대화를 전부 뒤진 뒤에만** "없음"을 말할 것
2. **전달 ≠ 커밋** — `present_files` 했다고 리포에 들어간 게 아니다. 전달본과 커밋본의 md5 대조가 필요
3. **검증기 자신에게도 §7.1 적용** — 오탐 51건짜리 검증기는 무시당한다. 올바른 것을 측정해야 한다
4. **네임스페이스는 초기에 고정** — 로그가 비어 있을 때만 바꿀 수 있다

### 3-3. ⚠️ 반복 제안 2회 (미착수)

같은 제안이 **다른 세션에서 두 번** 나왔고 둘 다 착수 안 됐다. 그 사이 사고 2건 발생.

| 시점 | 제안 |
|---|---|
| 2026-09-03 | `--check-project-sync` — 프로젝트 지식과 리포를 SHA로 비교 |
| 2026-09-04 | `MANIFEST.txt` — 각 파일 md5를 매니페스트로 남김 |

**콘텐츠 판정에는 "반복 3회면 승격" 장치가 있는데(신규 규칙 후보), 인프라 판정에는 없다.** 그래서 같은 제안이 두 번 나와도 아무도 세지 않았다. → §13-3의 15번(`infra-decisions.md`) 제안 배경.

---

### 3-1. 발생한 사고 5건

| # | 사고 | 정체 | 발견 경로 |
|---|---|---|---|
| 1 | UTF-8 바이트 2개 유실 (`C가`→`C\xb0\x80`, `C를`→`Cꥼ`) | 인코딩 손상 | 디코딩 실패로 자동 발견 |
| 2 | **solfa 자리에 pdp 사본** (6파일 md5 동일) | 이름은 맞는데 **내용이 다름** | Claude Code가 다른 작업 중 우연히 |
| 3 | **02-gate.md 구버전 커밋** (G-R 누락) | 이름도 내용도 있는데 **버전이 다름** | Claude Code 보고와 프로젝트 지식 대조 |
| 4 | **게이트 P-10 vs 규칙 P-10** | **같은 이름이 두 뜻** | 보고받은 다른 건 확인 중 발견 |
| 5 | README가 없는 `prompts/` 참조 | 이름은 있는데 **대상이 없음** | Claude Code 보고 |
| 6 | **"05-provenance 영구 결번" 오판** | **"찾지 못함"을 "없음"으로 접음** | `/summary` 실행 중 과거 대화 검색으로 |

**전부 "이름과 실체의 불일치"이고, 전부 `§n-m` 댕글링 검사를 통과했다.** 그 검사는 문서 안의 논리적 참조만 봤기 때문이다.

### 3-2. 도출된 규약

1. **§7.1 재확인** — 사고 6이 정확히 이 규약 위반이었다. 파일이 안 보인다고 없다고 판정하지 말 것. **작업 디렉터리·트랜스크립트·과거 대화를 전부 뒤진 뒤에만** "없음"을 말할 것
2. **전달 ≠ 커밋** — `present_files` 했다고 리포에 들어간 게 아니다. 전달본과 커밋본의 md5 대조가 필요
3. **검증기 자신에게도 §7.1 적용** — 오탐 51건짜리 검증기는 무시당한다. 올바른 것을 측정해야 한다
4. **네임스페이스는 초기에 고정** — 로그가 비어 있을 때만 바꿀 수 있다

### 3-3. ⚠️ 반복 제안 2회 (미착수)

같은 제안이 **다른 세션에서 두 번** 나왔고 둘 다 착수 안 됐다. 그 사이 사고 2건 발생.

| 시점 | 제안 |
|---|---|
| 2026-09-03 | `--check-project-sync` — 프로젝트 지식과 리포를 SHA로 비교 |
| 2026-09-04 | `MANIFEST.txt` — 각 파일 md5를 매니페스트로 남김 |

**콘텐츠 판정에는 "반복 3회면 승격" 장치가 있는데(신규 규칙 후보), 인프라 판정에는 없다.** 그래서 같은 제안이 두 번 나와도 아무도 세지 않았다. → §13-3의 15번(`infra-decisions.md`) 제안 배경.

---


---

## 4. 세션 로그

> 세션마다 한 줄. 무엇을 했고 무엇이 해소/발생했는지.

| 날짜 | 세션 | 주요 작업 | 해소 | 신규 |
|---|---|---|---|---|
| 2026-09-02 | 솔파 재구조화 | 솔파 6파일 완성 | — | — |
| 2026-09-03 | 프로젝트 설정 | 리포 커밋 3건, 스킬 생성 | — | **"05 영구 결번" 오판 발생** |
| 2026-09-03 | 프드프 아카이브 | pdp 6파일 + cross-decisions + 00-gate + eval-set | — | 네임스페이스 충돌 잠복 |
| 2026-09-04 | 복구·수정 | 솔파 6파일 원본 복구 / 네임스페이스 3층 충돌 해소 / 검증기 2종 / 지문 필드 / infra-decisions / SEED-LOG 분리 | L-13, L-15, L-29 | L-26~L-29 |
| 2026-09-04 | 평가 하니스 | 프롬프트 B-CC 작성 (스킬 품질 측정용). INF-04 반복 3회 도달, INF-05 신규 | — | L-30 |

---

## 5. 갱신 규칙

1. **append-only.** 항목을 지우지 않는다
2. 해소 시 `상태` 열만 `[해소 YYYY-MM-DD]`로 변경
3. 재발 시 **새 엔트리를 만들지 말고** 원 엔트리에 재발 행 추가
4. 세션 종료 시 §2 나이 갱신 + §4 세션 로그 한 줄 추가
5. **30일 초과가 5건 넘으면** §2-5 집계에 ⚠️ 표시하고 다음 세션 최우선으로 올린다

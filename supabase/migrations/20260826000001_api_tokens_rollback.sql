-- ============================================================
-- 20260826000001_api_tokens.sql 되돌리기
--
-- ⚠️ 이 롤백은 저장된 액세스 토큰을 영구 삭제한다.
--   Threads 토큰은 재생성할 수 없다 — 다시 받으려면 브라우저로
--   인가 창을 거치는 수동 재인증이 필요하다.
--   되돌리기 전에 값을 따로 확보하라(대시보드 결과창에 노출되므로
--   백업 테이블보다는 안전한 곳에 옮겨 적을 것):
--     SELECT provider, access_token, user_id, expires_at FROM public.api_tokens;
--
-- 코드보다 먼저 되돌리면 토큰을 읽는 크론·라우트가 42P01 로 실패한다.
-- ensureValidToken() 은 조회 실패 시 null 을 반환해 조용히 skip 하도록 짜여 있으므로
-- 발행이 멈출 뿐 500 은 나지 않지만, 그래도 코드를 먼저 되돌리는 편이 안전하다.
-- ============================================================

DROP TABLE IF EXISTS public.api_tokens;

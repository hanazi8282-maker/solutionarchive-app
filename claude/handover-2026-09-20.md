# 인수인계 — 2026-09-20 (Fable 5.1 대화형 세션, 남헌 통합 지시 PART A/B/C)

> 제1참모(Cowork) 아침 브리핑용. Notion 일일 상태 로그 `2026-09-20-기타` 와 같은 내용을 파일로 남긴다.
> 무엇을 반영했고 무엇을 뺐는지가 이 문서의 목적이다. 정본 링크: `docs/pmf-product-design.md`(설계) ·
> `reports/2026-09-20/pmf-product-research.md`(리서치) · `docs/migration-exceptions.md`(마이그 이력).

## 0. 한 줄
PART A 9건 전부 처리, PART B 거버넌스 개정, PART C 는 **로그인한 셀러가 프로젝트를 넣고 → 진단 → 결과 보고서 → 요약 복사까지 한 번에 가는 화면**이 main 에 들어갔다(PR #171~#176). 실DB 유산균 프로젝트로 진단→처방→요약→원문 인용까지 통합 검증했다. 화면 실물(로그인 뒤 375px)만 남헌 확인이 남았다.

## 1. PART A — 즉시 조치 9건 (전부 처리)

| # | 지시 | 결과 | PR |
|---|---|---|---|
| 1 | CG-1 발행 게이트 버그 | 작가 프롬프트가 `fact_check_grade` 를 보게 함(게이트 무변경, 자동 삽입 없음). 회귀: 09-16~19 스테이징 5건을 실DB 등급으로 재판정 → 판정 불변, 귀속 문구 없는 사실확인 C 초안(09-18 fab-com) 여전히 차단, 귀속 문구 제거 시 둘 다 차단 → **오탐 통과 0건**. 지시문의 "6건" 은 로컬·git 이력에 5건뿐(09-17·09-20 스테이징 없음) | #164 |
| 2 | 다나와 리뷰 수집 유지 + 발굴 엔진 검색 robots | 리뷰 수집 경로 **0줄 변경**. 발굴 엔진 `dsearch.php` GET 앞에 러너와 같은 `RobotsCache` 판정(fail-closed). 실측 robots 는 `/dsearch.php` 허용·Crawl-delay 10 → 크론 동작 동일(robots.txt 1회 추가 읽기만) | #165 |
| 3 | 크론 3개 실패 200 | 실패 1건 이상 → **500**(207 은 Vercel 이 성공으로 셈). 본문은 그대로. 첫날 빨간불은 "새 장애" 가 아니라 "있던 실패가 보이기 시작" 일 수 있다 | #166 |
| 4 | npm audit | next 16.2.7→16.3.5 + js-yaml → 취약점 0. 클린 빌드·eslint·핵심 셀프테스트 통과 | #169 |
| 5 | 죽은 라우트 삭제 | `threads/publish`·`sync-conversions`·`lib/threads/publish.ts` 삭제. 호출처 grep 0건, 주석·문서 4곳 갱신, auth 셀프테스트 기대치 7→5 | #167 |
| 6 | 셀프테스트 2개 진단 | **수정 안 함.** 둘 다 코드 회귀 아님 — 기대값 낡음(§4-1) | — |
| 7 | UI 검수 대기 | 손대지 않음. 남헌 375px 피드백 대기 | — |
| 8 | extract 실행 | 유산균 `4fcea3ff` 원문 278건 → 속성 6·성숙도 4·메타신호 true. 로그인 벽 때문에 추출 본체를 lib 로 빼고 운영자 CLI `scripts/analyze-extract-run.mjs` 신설. (PART C 검증 중 새 프롬프트로 재추출 → 속성 7, 인용 20문장) | #170 |
| 9 | HN Firebase C 종료 | 확정. 이후 언급 없음 | — |

## 2. PART B — 거버넌스
- `CLAUDE.md §10.2` "마이그레이션 적용 · PR 머지를 누가 결정하나(2026-09-20 개정)": 기준을 "물을 수 있는가" → "되돌릴 수 있는가". 사람 판단 예외 5개(되돌리기 어려운 삭제 · 프로덕션 데이터 손상 · 키 노출 · 새 법적 리스크 · 사업 방향). 자율 승인은 한 방향(기존 명시 지시를 위험 판단으로 되돌리지 않음). Notion 기록 필수(누락 시 회수). — #168
- **이 세션의 자율 승인 기록**(Notion `2026-09-20-기타` 비고에도 있음): PR #164 #165 #166 #167 #168 #169 #170 #171 #172 #173 #174 #175 #176 머지(전부 CI build pass 확인 후) · 마이그레이션 `20260923000001_pmf_product_facets.sql` 자체 적용(추가만, information_schema 사전 실측 → 양성 6/1/1 → DO 블록 음성 check_violation 2건 → `docs/migration-exceptions.md` 기록).
- 판단 근거 한 줄: 전부 "되돌릴 수 있는 변경"(코드·추가 컬럼·문서). 삭제는 도달 불가 라우트 2개뿐이고 git 으로 복구 가능. 예외 5개 해당 없음.

## 3. PART C — 근거 수집을 제품으로

### 반영한 것 (main 에 있음)
- **리서치** `reports/2026-09-20/pmf-product-research.md`(#173) — 경쟁사 4(Foreplay·MagicBrief·Atria·Mirr) · 레퍼런스 6 · 차용 18 · 기각 12. 09-19 노트 정정 2건(Atria 등급 엔진은 Raya 가 아니라 Radar; Mirr 랜딩·도움말 불일치). 10곳 중 **원자료를 손으로 붙여넣게 하는 곳은 0곳** → 우리 2단계가 최대 구조 격차.
- **설계 정본** `docs/pmf-product-design.md`(#171) — 필수 3종을 `/analyze/[id]/result` 한 장에.
- **스키마**(#171, 적용 완료) — `analysis_projects` 패싯 6컬럼 · `analysis_aspects.evidence_quotes` · `seller_profiles`.
- **공용 판정 어휘**(#172) — `aspectVerdict`(I·S 만으로 여기를 민다/기본기/버린다/지켜본다) · `opportunityBreakdown` · `MATURITY_STAGES`(단계별 행동) · `PMF_QUADRANT_ADVICE`(사분면 처방).
- **결과 화면 + API**(#176) — `/analyze/[id]/result`: 한 줄 결론(사분면+처방) · 진단 실행 카드(패싯 인라인 입력 → `PATCH /api/analyze/projects` → `POST /api/analyze/pmf`) · 시장 성숙도 해설 · 두 축(선례축 MatchWhy·제외 건수, 수요축 퍼센타일은 표본 30 미만이면 안 냄) · 상위 소구점 3(분해·판정·원문 인용) · **문제 해결 제안**(`GET /api/analyze/remedy`, `lib/cases/remedy.ts` — 페인 속성마다 보완 선례≤3 / 막힌 사례≤2 / 원칙≤2) · 어드바이저 3버튼 · 요약 마크다운 복사(`GET /api/analyze/summary`) · "여기까지가 초안이다" 고지. 추출 프롬프트가 원문 인용을 요구하고 **저장 전 원문 포함 검사**로 지어낸 인용을 버린다. CLI `pmf-assess.mjs` 도 같은 행 조립 함수 사용.
- **기존 화면**(#174) — 검수 카드에 판정 배지·점수 분해·인용·"다음 미확인 속성 ↓"·실측 소요시간(`?stats=1`, 표본<5 → 측정 전) · PMF 패널 처방·마지막 진단 시각 · 앵글 고지 1줄 + 판정 어휘 통일 · 어드바이저 3버튼(`focus`) · 퀴즈 결과 CTA "이제 내 상품으로 해보기"/"먼저 남의 사례 구경하기" · `app/error.tsx` 3버튼.
- **온보딩·목록·프로필**(#175) — `/analyze/new` 1단계 패싯 6개 + 프로필 프리필 + "프로필로 저장", 2단계 **다나와 URL 로 등록하고 밤에 수집**(`/api/analyze/targets`) 경로 · `/settings/profile` + `GET|PUT /api/profile`(세션 이메일) · `/analyze` 빈 상태(→/cases) · 발굴 배너(조회 실패면 안 그림) · 오늘 볼 것 1건 · 오래 멈춤 배지 · 정체 한 줄.
- **통합 검증(실DB, 유산균 4fcea3ff)**: 패싯 입력 → `runPmfAssessment` → 진단 행 저장(수요축 0.55 · 선례축 0.00 · 사분면 UNCHARTED_DEMAND · TRUST 선례 0건은 **미승인 13건 제외**라서) → 처방 카드 2장(부작용/체질 · 가격) → 요약 43줄 → 재추출로 속성 7개·인용 20문장(전부 원문 포함 검사 통과). 셀프테스트 신규 8종(pmf-run 32 · remedy 25 · summary 27 · aspect-verdict 25 · extract-stats 20 · facets 71 · list-signals 33 + 기존) CI 배선.

### 뺀 것 (모순·불가 — 리서치 C-3 그대로)
단일 종합 점수 · 광고 소재/스토리보드 · 성과 기반 채점(매출 없음) · 예측 시뮬레이터(라벨 없음) · 소셜 계정 연결 · 브라우저 확장 수집(SP-019/020/021 충돌) · 실시간 수집(§2) · 크레딧 미이월(SP-002 안티패턴) · 공개 URL 대시보드(§5-1 권한 없음) · 지표 커스터마이즈 · 앱 안 회고 계층.

### 알고 있는 약점 (다음 라운드에서 걷어내거나 고칠 후보)
- **처방 매칭이 낱말 겹침**이라 얕다 — '부작용' 페인에 Homejoy OPERATIONS 무브가 "보완 선례" 로 붙고, 실패 사례 절반이 "신뢰도 낮음" 배지다. 카테고리 태그/임베딩 매칭으로 올리기 전까지는 `MatchWhy` 배지가 유일한 방어선.
- TRUST 병목 선례 0건은 데이터 문제(Tuft & Needle·Whole Pantry 등 13무브가 **미승인**) — `/cases` 에서 사람이 승인하면 바로 선례축이 산다.
- `/analyze/new` 2단계 붙여넣기는 여전히 남아 있다(URL 경로가 나란히 생겼을 뿐). 야간 수집이 도는 소스는 다나와 하나.
- 화면 실물·375px·클릭 검증은 이 세션이 못 했다(Google 로그인 벽, 워크트리에서 Turbopack 정션 빌드 불가 — CI·Vercel preview 는 전부 pass).

## 4. 사람 판단 필요 (Notion 사람판단필요=true)
1. **A-6**: `strategy-principles-sync`(기대 30행·끝 SP-031 vs 원장 31행·SP-032, 09-18 PR #152) · `failed-angles-sync`(기대 5~10행 vs 원장 12행, 09-12 PR #49). 파서 정상(둘 다 `--dry-run` 끝까지 파싱). 부작용: 테스트가 빨개서 sync 를 안 돌려 **DB 가 원장보다 뒤처짐** — strategy_principles DB 29행(SP-030~032 없음, SP-029 는 DB 에만), failed_angles DB 22 vs 원장 12. 어드바이저·처방은 DB 를 읽으므로 SP-030~032 가 화면에 안 나온다. → 기대값을 원장에 맞출지 / 원장 상한을 지킬지 + DB↔원장 정리 방향.
2. "이커머스 PMF 발굴 SaaS 경쟁조사" 문서 위치 — Notion·로컬·아티팩트 어디에도 없어 09-19 노트를 베이스로 진행. 위치를 알려주면 갭 분석에 반영.
3. UI 검수(7번) 피드백 + 이번 PART C 화면 실물 확인: `/analyze` → 유산균 프로젝트 → `/analyze/4fcea3ff-a930-4017-b301-888423284c86/result` 를 375px 로 한 번. 진단 실행 카드는 이미 패싯이 있어 "다시 진단" 으로 뜬다.
4. TRUST 병목 미승인 무브 13건 승인 여부(`/cases`) — 승인되면 유산균 프로젝트 선례축이 0 에서 올라간다.

## 5. 다음 세션 착수 순서
1. 남헌 화면 피드백 반영(7번 + result 화면).
2. A-6 결정에 따라 기대값/원장 정리 + `strategy-principles-sync`·`failed-angles-sync` 실행 → 어드바이저에 SP-030~032 반영.
3. 처방 매칭 품질: 카테고리 태그 기반 2차 필터 + `remedy-selftest` 에 오탐 사례 고정.
4. 걷어내기 라운드: result 화면 섹션별 사용 여부를 보고 필요 없는 것 제거(남헌 원칙 "일단 전부 구현 → 걷어내기").

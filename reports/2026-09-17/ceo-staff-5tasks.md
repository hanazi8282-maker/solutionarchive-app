# CEO-STAFF 세션 — 남헌 지시 5건 (2026-09-17)

작업 위치: `C:\Users\DCU\Desktop\Cowork\SolutionArchive` · 브랜치 `main` (`dfc394c`, origin/main 동기)
세션 시작 시 위치는 `Cowork`(git 리포 아님) — 리포로 이동해 진행했다.
DB 쓰기는 전부 이 세션(오케스트레이터)이 `.env.local` 자격증명으로 직접 했다. 서브에이전트 미사용.

---

## 1. 크론 미발화 원인 + 토큰 만료 (트랙 CTO)

**원인: 버그가 아니다. 그 시각에 워크플로가 기본 브랜치에 없었다.**

- `nightly-discovery.yml` 이 main 에 처음 올라온 시각 = **2026-09-16 18:38:37Z** (PR #128 머지 `eb19c54`).
- 예정 슬롯 **2026-09-16 17:13Z** 은 그보다 **1시간 26분 앞**이다. GitHub Actions 는 기본 브랜치에 있는 워크플로만 스케줄로 돌린다 — 발화할 대상 자체가 없었다.
- 크론식 `13 17 * * *` 는 정상(정각 회피 규칙 지킴). 워크플로 상태 `active`. 문법 오류 없음(같은 파일로 `workflow_dispatch` 6회 실행됨).
- **Actions 스케줄러는 그날 정상이었다**(교차 확인): 같은 밤 `Nightly Review Collect`(17:37Z 예정)가 20:22:48Z 에 실행됐다. 다만 **2시간 45분 지연**됐다 — 이 리포의 야간 크론은 상시 1~3시간 지연된다는 뜻이고, 감시 임계값(90분)보다 크다.
- 결론: 09-17 02:13 KST 슬롯(=09-16 17:13Z)은 **영영 오지 않은 것이 정상**이고, **첫 스케줄 발화는 2026-09-18 02:13 KST**(09-17 17:13Z)다.

**크론 감시(Cron Watchdog)는 이걸 이상으로 잡았다** — 09-16 23:53Z 실행 실패, Notion `2026-09-17-CTO-2` 행 생성. 탐지는 맞았지만 판정이 틀렸다(§7.2: 안전장치가 걸린 것 ≠ 안쪽 로직이 옳다).

**조치**: 근본 원인이 "고칠 것이 없음"이라 수동 실행으로 마감했다. `--dry` 가 아닌 **적재 모드**로 main 에서 실행 → run [35232111372](https://github.com/hanazi8282-maker/solutionarchive-app/actions/runs/35232111372), success, 후보 2건 채택·적재.

**Threads 토큰**: 만료 `2026-10-25T13:42:07Z` — 잔여 **37.98일**. 경보선(6일)까지 한 달 넘게 남았다. 재인증 불필요. `updated_at` 2026-08-26(갱신 임계값이 만료 7일 전이라 정상).

---

## 2. 발굴 엔진 첫 후보 검증 — 2건 전부 유지 (트랙 CTO)

스케줄 실행이 없었으므로(작업 1) 검증 대상은 **작업 1에서 수동 실행한 적재 모드 실행 결과**다. 09-17 04:22~04:42Z 의 기존 dispatch 6회는 전부 `--dry`(기본값 true)라 **DB 쓰기 0건**이었다 — 그 후보(다이슨·하기스)는 DB 에 존재하지 않는다.

- **필립스 에어프라이어 XXL** — danawa pcode 10887642 "필립스 스마트 센싱 HD9860/95" reviews=93 / 검색결과 3건 · 판정 accepted(93 ≥ 30) · 사람 결정 **kept**
- **오랄비 전동칫솔** — danawa pcode 4825982 "브라운 오랄비 스테이지스 파워 키즈 D2" reviews=999+(하한) / 검색결과 40건 · 판정 accepted(999 ≥ 30) · 사람 결정 **kept**

- 둘 다 실측 hits 가 최소 기준을 넘겼고, 프로브 ref 가 실재하는 상품이며 카테고리도 후보명과 일치한다. 무효(killed) 사유 없음.
- 재확인(§7.1 양성): `discovery_candidates.human_review` 두 행 모두 `kept`, `verdict` 는 손대지 않음. `review_targets` active 5건 중 신규 2건 포함, `analysis_projects` 2건 생성.

**검증에서 걸러내지 않고 남긴 것(막힌것 대상)**

1. **게이트에 상한선이 없다.** `DISCOVERY_MIN_VOC_HITS=30` 하한만 있고 상한이 없어, Notion(79,072건)·Heroku(25,461건)·다이슨·하기스 같은 초대형 브랜드가 그대로 통과한다. 이번 2건도 필립스·오랄비(P&G) — **글로벌 대형 브랜드다.** 지시대로 무리하게 걸러내지 않았다.
2. **프로브 ref 대표성.** "오랄비 전동칫솔" 후보에 프로브가 고른 상품은 **키즈용 모델**이고, `review_targets.product_ref` 도 그 pcode 다. 수집은 정상적으로 돌지만 모이는 VOC 는 성인 프리미엄 모델의 것이 아니다. 후보명과 실제 수집 대상이 어긋나는 첫 사례다.
3. **오늘 밤 이중 실행.** 수동 적재 1회를 이미 했으므로, 09-18 02:13 KST 첫 스케줄 발화가 더해져 오늘 하루 후보가 4건이 된다(중복 게이트는 DB 를 열고 도니 정상 작동).

---

## 3. 반려 30건 재검토 — 승인 3건 → 3건 (0건 승격) (트랙 CMO)

**재채점 결과: 등급이 바뀐 무브 0개.** 현행 코드로 실제 재계산을 돌린 결과다.

- `node scripts/case-review.mjs regrade --dry` — 무브 72개 / 인사이트 등급 변동 **0개** / 사실확인 등급 변동 **0개**.
  - 인사이트 A7 · B0 · C3 · D62 (전·후 동일) · 사실확인 A22 · B1 · C42 · D7 (전·후 동일)
  - 그중 9개는 **잠정**(관측 키 미기재로 교차확인 판정 불가) — "근거가 약하다"가 아니라 "확인하지 않았다"다.
- **왜 하나도 통과하지 못하나**: 반려 30건에 속한 무브 62개의 `transfer_note` 가 **전부 빈칸**이다(62/62). `gradeMove()`(lib/cases/draft.ts:332)는 `transfer_note` 가 없으면 다른 걸 보기도 전에 **D** 를 준다. 산식이 낡아서가 아니라 **원자료에 독자 전이 필드가 없다.**
- 사실확인 축은 오히려 튼튼하다 — 반려 30건 중 blue-apron(AAA)·oatly·peloton·elf·purple·pets.com(AA) 등은 사실확인 A다. **떨어진 이유는 "사실이 부실해서"가 아니라 "독자가 내일 할 행동이 안 적혀서"다.**
- 09-16 에 이미 같은 기준으로 재검수해 반려한 이력이 각 행 `review_note` 에 남아 있다. 오늘 재채점은 그 판정을 독립적으로 재확인한 것이다.

**조치**: 지시대로 30건 전부 반려 유지. 개수를 채우려고 새 케이스 만들지 않았다.
**전/후 건수**: 35건 중 approved **3 → 3** (rejected 30 → 30, draft 2 → 2). 재확인 완료.

승격 가능한 유일한 경로는 **30건의 `transfer_note`·`preconditions` 백필**(=사실상 재작성)이고, 그건 채점이 아니라 CMO 의 작성 업무다.

---

## 4. hoka 케이스 검수 — 불통과, DB 미적재 (트랙 CMO)

`drafts/cases/hoka-specialty-retail-awareness-engine.json` (HOKA / Deckers Brands, 무브 2개, 근거 8건)

**걸린 기준**

- `validateDraft` 경고 3건: `reader_problem` 비었음(**선정 1순위 축**), `moves[0]`·`moves[1]` **`transfer_note` 없음**.
- `gradeMove` 실측: 무브 0(CHANNEL) **인사이트 D**, 무브 1(CONTENT) **인사이트 D** — 사유 "transfer_note 미기재 — 독자가 할 행동이 없다".
- `preconditions` 도 두 무브 모두 빈칸.
- 사실확인 축은 **A/A**다(무브 0: 법정 공시 3건, 무브 1: 비자기보고 1차 출처 1건). 근거는 강한데 전이가 없다 — **작업 3의 반려 30건과 정확히 같은 결함**이다.

**조치**: DB 미적재. `case_studies` 에 hoka 행 **0건**(재확인 완료, 총 35건 유지). "6일째 미적재"는 사고가 아니라 지금 기준으로는 **적재되면 안 되는 상태**였다.

---

## 5. 미연결 Threads 게시물 — 연결하지 않았다 (맞는 항목이 없다) (트랙 CMO)

**대상 특정 완료**: Threads id `18165008242467071`, 발행 2026-09-16T17:48:28Z(= 09-17 02:48 KST), 498자, permalink `https://www.threads.com/@solution_arch_/post/DdW2785gQNu`. 지금 기준 경과 **20.4시간**(digest 작성 시점엔 5시간이었다 — `reports/2026-09-16/DIGEST.md:10`).
매처 최신 기록(09-17 14:00Z): `unlinked=1`, `threads_checked=3`, `apply_failed=0` — 수치는 신선하다.

**연결하지 않은 이유: 대응되는 초안이 DB 에 없다.**

- 발행본은 **조선미녀 천주혁 대표가 중계업으로 데이터를 모아 브랜드를 인수했다**는 이야기다.
- `posts` 27행 전체와 매처의 실제 산식(`normalizeBody` + 바이그램 Dice)으로 대조 — **최고 유사도 0.103**. 2위 0.089. 매처 임계값에 한참 못 미친다.
- 같은 브랜드 초안 `CS-20260910-02`(0112da55, pending_review)는 **"국내→해외 유통 순서 역전 + 매출 400억→3237억"**으로 주장·수치·무브가 전부 다르다. 이 글이 다루는 "인수" 서사를 담은 `case_moves` 는 DB 에 없다.
- 억지로 연결하면 (a) 미발행 초안이 `published` 로 뒤바뀌고, (b) `body` 가 무관한 본문으로 덮어써지고, (c) 그 글이 말하지 않은 무브에 성과 지표가 귀속된다. `app/dashboard/actions.ts` 의 수동 연결 주석도 **"어느 초안의 발행본인지 아는 건 사람뿐이다"**라고 못박는다.

**남헌이 정할 것 (아래 "결정 필요" 3번)** — 사람이 직접 쓴 글을 대시보드에 어떻게 취급할지. 그 답이 없으면 매일 digest 에 같은 경고가 계속 뜬다.

---

## 결정 필요

1. **반려 30건 전이노트 백필을 시킬지.** (A) 시킨다 — CMO 에 30건 `transfer_note`·`preconditions` 작성 발주, 통과분만 재승인. (B) 안 시킨다 — 신규 작성분(09-14 이후 방식)만 쌓아 발행 재료를 만든다. **권고: B 우선 + A 는 사실확인 A 등급 6건(blue-apron·oatly·peloton·elf·purple·pets.com)에만 한정.** 근거가 이미 튼튼한 건만 전이노트를 붙이면 회수가 가장 크다.
2. **발굴 게이트 상한선.** (A) hits 상한 도입(예: 5,000 초과 제외) (B) "글로벌 대형 브랜드 제외"를 LLM 프롬프트 제약으로 (C) 그대로 둔다. **권고: A** — 프롬프트 제약은 실측이 아니라 주장에 의존해 이 엔진의 전제를 깬다.
3. **사람이 직접 쓴 Threads 글의 자리.** (A) `posts` 에 발행 기록만 남긴다(성과 수집 대상에 포함, unlinked 0으로 내려감) (B) 연결하지 않고 둔다(매일 경고 유지) (C) 매처가 "파이프라인 외 게시물"을 따로 표시하게 고친다. **권고: C → 당장은 A.** B 는 경고가 상시화되어 다음에 진짜 미연결이 생겨도 아무도 안 본다.
4. **크론 감시 임계값.** 이 리포의 야간 스케줄은 상시 1~3시간 지연된다(09-16 Review Collect 2시간 45분). 90분 임계값은 오탐을 계속 낸다. (A) 임계값을 4시간으로 (B) "워크플로가 그 슬롯에 기본 브랜치에 있었는지"를 먼저 보게 고친다. **권고: A+B 둘 다** — 오늘 오탐은 두 원인이 겹쳤다.

## 개선안 (지금 구조에서 바꿀 것)

- **크론 감시에 "워크플로 도입일" 조건을 넣는다.** 오늘 실패는 존재하지 않던 워크플로의 미발화를 이상으로 보고한 것이다. 새 야간 워크플로를 추가할 때마다 첫날 오탐이 뜬다.
- **`case-review.mjs commit` 이 인사이트 D 무브만으로 이뤄진 초안을 막아야 한다.** hoka 는 6일 동안 "적재 대기"로 보였지만 실은 통과 불가였다. 실패를 대기처럼 보이게 하는 상태다(§7.1).
- **발굴 프로브가 고른 ref 를 후보명과 대조해야 한다.** "오랄비 전동칫솔 → 키즈 모델"처럼 이름과 실제 수집 대상이 어긋나도 지금은 아무도 모른다.

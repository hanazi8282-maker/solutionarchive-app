# 인수인계 — 2026-09-21 (2) Fable 5.1 대화형 세션, 남헌 승인 후속 5건

> 제1참모(Cowork) 아침 브리핑용. 앞 문서는 `claude/handover-2026-09-21.md`(09-20 후속 3라운드). 이 문서는 그 §3 "남헌 결정 대기 4건"이
> 승인된 뒤 처리한 후속 5건만 다룬다. Notion 일일 상태 로그 `2026-09-21-기타-4` ~ `-8`(항목당 1행)과 같은 내용이다.

## 0. 한 줄
승인 5건 중 4건 완료·머지(PR #183~#187, 자율 승인), 1건(extract)은 실측 근거로 실행하지 않고 자율 기준을 세웠다. 사람 판단은 네이버 1건 + YouTube 운영 3가지가 남았다(§3).

## 1. 반영한 것

| # | 결정 | 결과 | PR / DB |
|---|---|---|---|
| 1 | 추정 6건 원장 편입 | 출처 URL 6건 재검증 뒤 16→22. halo-top은 기존 URL에 인용 수치가 없어 Food Dive 인수 기사 추가, beautycounter는 WWD 유료벽 → Yahoo 신디케이션·DSN 교차확인, 검증 불가한 "245→50 SKU" 삭제. 결과 화면 처방 카드에 '추정' 배지. 셀프테스트 22행, sync → DB 22건 | #183 · DB upsert |
| 2 | 선례축 negative 제외 | `matchMoves`에서 `outcome_direction=negative` 제외 + `excluded.negative` → `match_reason "· 반면교사 N"`. 처방 카드 반면교사 경고는 별도 경로라 무변경. 유산균 재진단 8건·3케이스 → **6건·2케이스, 선례축 1.00, PROVEN_DEMAND 유지** | #184 · pmf_assessments 1행 |
| 3a | PR #106 — YouTube | 어댑터·픽스처 4종·셀프테스트를 main 러너(#152 robots fail-closed)에 맞춰 이식. `robotsPolicy` 대신 `proceedWhenRobotsUnverified:['www.googleapis.com']`(robots 404 실측), `requiredEnv` 정식 필드 + 수집기 사전검사, 타깃 빌더(URL→`v:ID`), 워크플로·CI 배선, 마이그 `20260926000001`(DB에 이미 있는 행의 출처 기록, DO NOTHING). 로컬 키 실호출 200 | #185 |
| 3b | PR #106 — 네이버 | **구현 안 함.** 오픈API 실측 401 `Scopes are Empty`(개발자센터 앱에 검색 API 스코프 없음 = 우리 설정). 비API 경로는 cafe·search·section.blog·rss.blog robots 전부 `*: Disallow: /` + "RAG 목적 봇 금지" + 약관 자동화 수집 금지(SP-030) → 사람 판단 | Notion 사람판단필요 |
| 3c | PR #106 — Reddit | 보류. 실패 이력 = 자격증명 미발급(토큰 엔드포인트 401), 레이트리밋 아님. 코드는 브랜치에 남김 | — |
| 3d | PR #106 / SP-029 | #106 close(브랜치 유지 — 네이버·Reddit 원본). SP-029 원장 정식 편입 31→32행, 결번 예외 해제, DB sync | #187 |
| 4 | extract 실행 | **실행 안 함** — 마지막 extract 09-20 15:17 UTC 이후 신규 원문 0건(278건 전부 분석됨). 기준은 §4 | — |
| 5 | UI 실물 확인 | 375px에서 PMF 2축·처방·어드바이저·WTP 전부 정상, API 200, 콘솔 에러 0. WTP 옵션 클릭 → 제출 버튼 활성화 확인(제출은 안 함). 상단 네비 가로 넘침 발견·수정 | #186 |

자율 승인 기록(전부 CI pass 확인 후 머지): PR #183 #184 #185 #186 #187. 마이그 `20260926000001`은 파일만 추가(DB 행이 이미 있어 적용해도 DO NOTHING — 별도 적용 안 함). DB 쓰기: failed_angles upsert 22, strategy_principles upsert 32, pmf_assessments 2행(구 산식 검증 1 + 신 산식 1).

## 2. 뺀 것 / 하지 않은 것
- extract `--force` — 신규 원문 0건이라 재분석은 비용만 쓰고 속성이 비결정적으로 바뀐다(WTP 이력·결과 화면 기준이 흔들림).
- WTP "답 기록하기" 실제 제출 — 실기록이 남는다. 옵션 클릭→버튼 활성화까지만 확인.
- `review_sources.youtube` enabled=true 를 건드리지 않음 — 09-16 브랜치 마이그 적용분이고 누가 켰는지 미확인. 자율 승인은 한 방향(§10.2).
- Actions 시크릿 `YOUTUBE_API_KEY` 등록 — 크리덴셜 경계라 사람 몫.
- 처방 카드 반면교사 문장이 claim이 길면 문단이 되는 카피 문제 — 이번 범위 밖.
- 구 산식 검증으로 생긴 `pmf_assessments` 행 1건(fafda31f…) 삭제 안 함 — 데이터 DELETE는 사람 판단.

## 3. 남헌 결정 대기 (Notion `2026-09-21-기타-6` 사람판단필요=true)
1. **네이버** — (a) 개발자센터 앱에 '검색' API를 추가해 오픈API 경로로 갈지. 단 SP-025(오픈API 약관의 저장·가공 제한) 검토 선행. (b) 비API 스크래핑은 robots·약관 명시 금지라 권하지 않음.
2. **YouTube 운영 3가지** — Actions 시크릿 등록(없으면 야간 수집이 youtube만 건너뜀) · 댓글 켜진 영상으로 타깃 등록(`v:<ID>`, /discovery 빌더가 URL을 받음) · SP-026 30일 조건인 purge_apply 활성 확인. enabled=true 가 의도된 것인지도.

## 4. extract 자율 실행 기준 (09-21 세션 수립)
1. `status ≠ extracted` → 즉시 실행.
2. `status = extracted` → `extract_finished_at` 이후 신규 `analysis_inputs` **≥ 100건**이면 `--force` 재실행.
3. 그 미만은 대기. 실행 시 Notion 행에 "신규 N건" 기록. 비파괴 읽기·분석이라 §10.2 자율 범위.

## 6. 2라운드 — 남헌 승인 후속 4건 (같은 날, Notion `2026-09-21-기타-9` ~ `-12`)

| # | 결정 | 결과 |
|---|---|---|
| 1 | 네이버 예외조항 재확인(신청·구현 금지) | **예외 없음 확정, 네이버 VOC 폐기 최종 확정.** 근거 §6.1 |
| 2 | YouTube 운영 전체 활성화 | 시크릿 재등록(09-16부터 있었음) · 타깃 2건(댓글 142·944, 실측 확인) · purge 로컬 dry-run 0건 → `REVIEW_PURGE_APPLY=true` + `--apply` 첫 실행 0건 · Actions 실수집 성공(6요청·신규 560건) · 신규 ≥100 기준으로 extract 재실행(838건→속성 5개) · 재진단 PROVEN_DEMAND 유지 |
| 3 | enabled=true 감사 | **우발적/미승인에 가깝다.** §6.2. 코드·DB 무변경 |
| 4 | 처방 매칭 A/B | 14속성×82문서, 결과 애매 → 전면 적용 설계 안 함. `docs/remedy-matching-ab-2026-09-21.md`. 남헌 판단 대기 |

### 6.1 네이버 — 확인한 것
- 개발자센터 신규 신청 2026-07-31 중단(쇼핑·책·전문자료는 종료, 블로그·카페·지식iN 등 10종은 NAVER API Hub 이관, 기존 키 2027-06-30까지).
- 검색 API 특약 개정 2026-09-07 시행: "검색 결과를 AI에 입력하거나 학습·개선·평가·노출에 활용" 금지, 복사·저장·캐싱·제3자 제공·수익화 금지. 허용은 "네이버 검색 결과를 제공하기 위한 목적"뿐. **블로그·카페·지식iN에 별도 예외 없음.** 유일한 완화는 임시 저장(기기 24시간, 서버 열람 이력 21일) — AI 입력 금지와 무관.
- 인접 상품: 검색 트렌드·쇼핑 인사이트는 Hub에 남지만 상대 검색량·클릭 비율만 준다 — 본문이 없어 VOC 아님.
- 2026-08-31 카페 공지: 크롤러·봇 대량 수집 금지 + 수집분 삭제 요구 + 법적 조치 방침.
- 개발자센터 원문 페이지는 이 환경에서 접근 차단이라 뉴스웨이·데이터경제·토큰포스트·와플보드 4곳으로 교차확인했다.

### 6.2 YouTube enabled=true 감사
- **언제·누가**: 2026-09-16 CEO-STAFF(cowork-f3) 자율 세션. Notion `2026-09-16-기타-4` 에 "다모앙·82cook·보배드림·YouTube 활성화(6개 소스 가동중)" 로 기록. 시크릿 `YOUTUBE_API_KEY` 도 같은 날 07:09Z 등록(행 생성 07:23Z 보다 앞).
- **경로**: 마이그레이션 파일 없음(main·브랜치 어디에도 `enabled=true` UPDATE 없음), `docs/migration-exceptions.md` 기록 없음 → DB 직접 UPDATE. 09-18 okky·velog 적용 기록이 "enabled=true 6개 무변동"으로 관찰만 하고 지나갔다.
- **판정**: 우발적/미승인. 같은 날 `-2` 행이 "PR #106 머지 보류 확정"이라 어댑터가 main에 없는 상태에서 켰다 — 켜져 있어도 5일간 아무 일도 없었던 이유(실행 이력 0). 당시 §10.2(09-17 개정 전)로도 사람만 적용할 수 있던 시점.
- **재발 방지 제안**(코드 변경 안 함): ① 소스 on/off 는 `hackernews_enable` 처럼 마이그레이션 파일+롤백으로만, DB 직접 UPDATE 금지를 §10.1에 명문화. ② `review-migration-verify` 또는 `review-collect-status` 에 "enabled=true 인데 ADAPTERS 에 없는 키" 경보 1줄. ③ 시크릿 등록도 상태 로그에 남긴다(이번엔 기록 없이 등록돼 09-21 오전까지 "미등록"으로 알고 있었다).

## 7. 3라운드 — 처방 매칭 고도화 (Notion `2026-09-21-기타-13` · `-14`)

| # | 결정 | 결과 |
|---|---|---|
| 1 | 카테고리 필터 적용 | PR #190: `business_model` → physical/software 로 접어 선례 코퍼스 선행 필터, 제외 건수 노출, 셀프테스트 +13. **같은 14속성 재측정: 무관 82.9%→82.5%, 카드 1장 감소 — 효과 ≈ 0.** 낱말로 걸리던 SaaS 선례가 원래 1장뿐이었다 |
| 2 | 소비재 코퍼스 확장 착수 | `docs/remedy-corpus-expansion-2026-09-21.md`: 편향 정량화 + 후보 17건(선례 8·실패 9, 1차 출처 확보 4건) |

**진단 수정.** 편향의 축은 SaaS/물리가 아니다: ① 원칙 32건 중 27건이 파이프라인 운영 원칙(채널·법무·인프라)이라 셀러 처방과 구조적으로 무관 — A/B 원칙 관련도 0.00의 원인, ② 승인 물리 케이스 11곳이 매트리스·밀키트·의류·카메라라 뷰티·건기식 질의(효능·가격·향·용기)와 도메인 불일치(뷰티 1·건기식 0), ③ 실패 사례 22건은 이미 소비재 16건이지만 건기식 0. 다음 라운드 권고: 원칙 코퍼스를 셀러 화면에서 분리, Hims·Seed·락토핏·Care/of·Function of Beauty 우선 편입.

## 8. 4라운드 — 처방 매칭: 원칙 분리 · 소비재 편입 · 건기식 공백 (Notion `2026-09-21-기타-15` ~ `-17`)

| # | 결정 | 결과 |
|---|---|---|
| 1 | 원칙 코퍼스 셀러 화면 분리 | PR #192: 원장 태그 `seller`(5건)·`software`(3건)로 독자 분리, advise 기본 독자 seller. **같은 14속성: 무관 31/39(79.5%) → 19/27(70.4%)**, 빠진 12장 전부 무관 |
| 2 | 소비재 편입 5건 | failed_angles +2 (Care/of·Function of Beauty, 추정, PR #193) · case draft 3 (Native·락토핏·Hims, PR #194, **승인은 남헌 /cases**). Seed 보류(1차 출처 없음). 편입 후 재측정: **무관 22/37(59.5%)**, 카드 1.93→2.64/속성 |
| 3 | 건기식 공백 | `docs/remedy-corpus-expansion-2026-09-21.md` §2-4 에 "건기식 전용 사례 추가 조사 필요" + 후보 축 명시 |

하루 누적: 무관 비율 82.9%(원안) → 82.5%(카테고리 필터) → 70.4%(원칙 분리) → **59.5%**(실패 사례 2건 편입). draft 3건이 승인되면 선례 코퍼스도 처음으로 뷰티·건기식·헤어케어를 갖는다.

**남헌이 /cases 에서 볼 draft 3건**: `native-deodorant-reformulation-reorder`(무브 2) · `lactofit-mass-price-probiotics`(무브 2, ⚠️ "국내 1위"는 2020년까지 — 2021년 hy 에 내줌, mixed) · `hims-hair-loss-rx-subscription`(무브 1). 케이스+무브 둘 다 승인해야 매칭에 잡힌다. 확인 항목은 각 `drafts/cases/*.json` 의 `research_notes`.

## 9. UI 전면 개선 — /feature 루프 (Notion `2026-09-21-기타-18`)

남헌 지시: 경쟁 서비스 UI 공통 패턴 반영 + 좋은 요소 세션 판단 적용, 전 화면, 문제는 나중에 남헌 삭제. **머지는 남헌.**

| 단계 | 산출물 |
|---|---|
| 조사 | `docs/ui-competitor-patterns-2026-09-21.md` — 7곳(Appbot·Chattermill·Enterpret 강, SellerSprite·Kimola·Helium 10·아이템스카우트 중), 공통 패턴 11(요약 숫자 카드 맨 위·순위 테이블·행→원문·"n/N건·기간" 캡션·감성 색 규약·다음 행동 분리·사이드바 3:2 등) |
| 설계 | `docs/ui-redesign-plan-2026-09-21.md`(#197) — 데이터 없는 패턴(언급수·감성·추이·별점) 기각, 사이드바 ≥1024px, 브랜드 보라, PR 7분할, AC, 리스크(전체 🟡·검수 🔴), 검증 4겹 |
| 구현 | PR1 #196 → 스택 #199 #198 #201 #200 #202 → **통합 #203**(25 files, `lib/**` 0, `app/api/**` OG 색 2줄). 서브에이전트 5개 병렬(워크트리) |
| 검증 | 통합 브랜치 tsc·lint 0, build 30/30, 셀프테스트 16종, preview 익명(/login·/quiz 200, 보호 307). **화면 픽셀은 전부 미확인** — 로그인 벽 |

### 9.1 머지 (남헌 지시 "자체 검수 후 전부 머지", Notion `2026-09-21-기타-19`)
- 머지 전 자체 검수: `/code-review 203 high` → 지적 10건 전부 반영(커밋 5991127): sticky 필터 행이 `<header>` 안이라 무효였던 것 → 형제로 · `/login`·`/onboarding` 의 236px 빈 여백 → `body:has(.sa-sidebar)` 게이트 · 인용 캡션 "N/?건" → "N건"·분모는 같은 범위만 · 케이스 근거 0건 문구 명사 · 딥링크 `decodeURIComponent` URIError · 앵글/원문 카운트 병렬 · 프리필 배너 오탐 · AppNav 계정 블록 한 벌 · 문서 주석 갱신 · 점프 칩 미결정 수 경고색. 알려진 차이 3건(검수 2열 1024px·딥링크 행 선택·프로필 칸별 안내)도 같이 보완(커밋 2791c53).
- 통합 브랜치 재검증: tsc·lint 0, build 30/30, 셀프테스트 통과 → **#203 머지**(main 4b15daa). 스택 #196·#198~#202 는 GitHub 이 자동 머지 처리. 워크트리 21개·브랜치 정리.
- **prod 실물 검증(남헌 Chrome, 로그인 상태)**: 데스크톱 1536px — 사이드바 2그룹·목록 타일 4·sticky 필터 칩·결과 히어로(사분면·결론·두 축·미니 사분면)·검수 테이블+상세 패널·케이스 타일/칩/점프 목록·발굴·새 분석 단계 표시 전부 렌더 확인. **375px iframe 11개 라우트 전부 `scrollWidth === clientWidth`**(analyze·result·review·angles·new·cases·columns·discovery·dashboard·agents·profile), 사이드바 display:none·상단바 block.
- 남은 남헌 몫: 마음에 안 드는 요소 삭제 지시 · 검수 화면 저장 실클릭은 실데이터라 세션이 누르지 않음. (RLS 는 §10 에서 처리됨)

## 10. Supabase 어드바이저 보안 — RLS 긴급 + 뷰/함수 후속 (Notion `2026-09-21-기타-20` · `-21`)

### 10.1 seller_profiles · wtp_signals RLS (PR #206, 마이그 `20260927000001`)
- 남헌 긴급 지시. 두 테이블 RLS 비활성(critical) → ENABLE+FORCE, 소유자 정책 5개(`owner_email = lower(auth.jwt()->>'email')`, authenticated 전용, DELETE 정책 없음). service_role 은 bypassrls 라 앱 라우트(`app/api/profile`·`/api/analyze/wtp`)·배치 무영향.
- 스테이징 브랜치가 없어(list_branches 0) DO 블록 안에서 DDL+역할 전환 실측 후 RAISE 로 롤백하는 방식으로 사전 검증. 결과·양성·음성은 `docs/migration-exceptions.md` 참조. anon PostgREST → `[]`.

### 10.2 post_performance 뷰 DEFINER(ERROR) + 함수 search_path(WARN 2) (마이그 `20260927000002`)
- **판정: 의도적이 아님.** `20260827000001` 이 옵션 없는 `CREATE OR REPLACE VIEW` 였고 Postgres 기본이 정의자 권한이라 그렇게 됐을 뿐. 실효 피해: anon 키로 `post_performance` 를 치면 RLS 걸린 posts·metric_snapshots 의 성과 집계 35행이 그대로 읽혔다.
- 호출자 실측 전부 service_role: `lib/insight/loop.ts`·`lib/insight/patterns.ts`(server.ts) · `scripts/threads-report.mjs`(SERVICE_ROLE_KEY 필수) · `guard_hypothesis_promotion` 트리거(호출 롤로 실행). 브라우저 anon 클라이언트 import 0. → **SECURITY INVOKER 전환 + anon/authenticated GRANT 회수**. 트리거 함수 2개는 `SET search_path = public, pg_temp`.
- 검증: 적용 전 DO 블록 드라이런 → 적용 → 재실측 `anon=denied auth=denied service=35`, 가설 H1 supported 승격 시 "표본 0개" 예외 그대로, learnings confirmed/sample 1 예외 그대로. 셀프테스트 insight-patterns·threads-collect·threads-match·insight-loop-db-guard 통과. anon PostgREST `401 42501 permission denied for view`, service 200.
- **어드바이저 재조회: ERROR 0 · WARN 1(leaked password protection — Auth 대시보드 설정, 남헌 몫) · INFO 37(정책 없는 RLS = service_role 전용 관례, 의도).**
- 남은 사람 몫: Auth → Password → "Leaked password protection" 토글(코드로 못 함). 이 앱은 Google SSO 만 쓰므로 실효는 낮다.

## 11. 09-22 — 리뷰 소스 확충 + 처방 매칭 권고안 E 착수 (남헌 승인, Notion `2026-09-22-기타-1`)

### 11.1 리뷰 소스 다변화 (PR #211, 마이그 `20260928000002/3/4` 적용됨)
- **꺼져 있던 8곳을 3차로 나눠 전부 켰다.** 1차 clien·theqoo·todayhumor → 수동 실행 신규 606건 확인 → 2차 fmkorea·brunch·tumblbug → 330건 확인 → 3차 okky·velog(개발자 커뮤니티라 지금 프로젝트와 겹치는 글이 없어 **타깃 0** 으로 켜 둠. SaaS 프로젝트가 생기면 등록).
- **"켜져 있는데 타깃 0" 이었던 4곳 원인**: 커뮤니티 소스의 타깃은 **글 1건 = 타깃 1건**(`url:<경로>`)이고 등록 경로가 사람이 `/api/analyze/targets` 에 글 주소를 붙여넣는 것뿐이다. 발굴 엔진은 danawa·hackernews 만 프로브한다(`scripts/discovery-run.mjs` PROBE_SOURCE). 09-16 에 소스를 켠 사람이 글 주소를 등록하지 않았고, 그 뒤 아무도 안 했다. 소스 플래그는 허가증이지 수집 지시가 아니다.
- **타깃 등록**: 커뮤니티 52건(clien 19 · 82cook 7 · brunch 7 · fmkorea 5 · theqoo 4 · todayhumor 3 · bobaedream 3 · damoang 3 · tumblbug 1) + YouTube 17건(활성 프로젝트 8곳: QCY·소니·오랄비·코웨이·바디프렌드·라보에이치·TS·락토핏·스토케·CJ 유산균, 댓글 35~3,388개 영상). 전부 `lib/review/target-ref.ts` REF_BUILDERS 로 검증 후 삽입(스크립트는 scratchpad, 1회성).
- **실측 결과(수동 실행)**: 커뮤니티 9소스 신규 1,054건(clien 500 · fmkorea 323 · todayhumor 102 · damoang 48 · 82cook 37 · bobaedream 33 · brunch 6 · theqoo 4 · tumblbug 1). YouTube 신규 **8,621건**(수동 실행 26분, 17타깃·요청 94건, 파싱 실패 3). 파싱 실패 clien 6·todayhumor 1·brunch 1(@sptco/14 0건) — 표본 대비 소수, 삭제 댓글로 추정, 별도 조사 안 함.
- 커뮤니티 타깃은 글이라 한 번 읽으면 **exhausted** 가 정상이다. 계속 들어오게 하려면 글을 계속 등록해야 한다 — 다음 단계는 발굴 엔진이 커뮤니티 검색으로 글을 찾아 타깃을 만드는 것(미착수, 백로그).
- **야놀자 제외·티스토리 보류**(남헌 09-22) — 둘 다 등록된 적 없고 건드리지 않았다.
- 알려진 것: 나이트리 워크플로는 소스당 `--targets=10` 이라 clien 19건 같은 경우 이틀에 걸쳐 돈다. 오늘은 수동 `--targets=50` 으로 전부 읽었다.

### 11.2 처방 매칭 권고안 E — 1단계 LLM 게이트 (PR #209 머지, 마이그 `20260928000001` 적용됨)
- `remedy_verdicts` 캐시 테이블 + `lib/cases/remedy-gate.ts`(순수)·`remedy-judge.ts`·`remedy-db.ts` + `POST /api/analyze/remedy/judge` + extract 끝 best-effort 판정 + `scripts/remedy-judge.mjs --all`. 화면은 속성마다 "재검사: N장 통과 · M장 제외 · K장 미검증", 미검증은 배지만 붙고 사라지지 않는다(§7.1).
- **백필 결과(gemini-3.5-flash-lite)**: 10프로젝트 후보 58장 → 판정 58 · 무관 37(63.8%) 제거 · 통과 21 · 미검증 0. 화면에 남는 카드 21장.
- **남헌 채점 표본**: `reports/2026-09-22/remedy-grading-sample.md`(+csv). 게이트 통과 모집단이 21장뿐이라 **30장이 아니라 21장 전수**다. 관련/무관 체크만 하면 된다. 채점 결과를 `human_verdict` 로 되돌리는 import 스크립트는 아직 없다(채점 받은 뒤 만든다).
- ⚠️ 표본 #14 는 `(mock)` 속성이다 — 3주 만에… 프로젝트에 mock 속성이 남아 있다. 채점에서 빼도 된다.

### 11.3 소비재 사례 확충 착수 (PR #210 머지, DB 반영 완료)
- **페인 낱말 규칙**: `config/pain-terms.json`(8유형) — 새 실패 사례 `claimed_angle`·새 케이스 무브 `claim` 에 낱말 1개 이상 없으면 validate/sync 가 거부. 기존 24행·45초안은 `legacy_*` 면제.
- **편입**: failed_angles 24→**30**(Flower Beauty·Pat McGrath·Cover FX/Mally·Glossier 매장·SK네추럴팜 유산균 함량 미달·아이배냇 어린이 유산균). case draft **+6**(AG1·Ritual·Seed·Dr. Squatch·hy 야쿠르트·고려은단) → `/cases` 에 draft 14건 대기(이전 3 + 오늘 6 + 기존 5).
- 누적 17/32. 보류: Prose(후퇴 근거 없음)·식약처 과장광고(제품명 없음)·Liquid Death(기존 초안 있음).
- 게이트 백필은 편입 **뒤에** 돌렸으므로 새 실패 사례 6건도 판정에 들어갔다.

### 11.4 남은 사람 몫
- `/cases` draft 14건 승인(승인돼야 선례 코퍼스에 들어간다 — 지금 뷰티·건기식 선례가 0).
- `reports/2026-09-22/remedy-grading-sample.md` 21장 채점 → 그 결과가 "10% 달성" 판정의 근거.
- 커뮤니티 글 지속 등록 방법(발굴 엔진 확장) 착수 여부.

## 5. 운영 메모
- 이 머신의 `python3`/`python`은 Windows 스토어 스텁이라 아무것도 안 하고 "Python"만 찍는다. 파일 치환은 node로.
- 리포 파일 대부분 CRLF — 다중행 문자열 치환은 `\r\n`→`\n` 정규화 후 되돌려 쓴다.
- Chrome 창이 최대화면 `resize_window`가 안 먹는다. 같은 오리진 iframe(375px)이 모바일 재현에 충분했다(쿠키 동일, 미디어쿼리 적용).
- 네이버 오픈API `Scopes are Empty`는 키가 틀린 게 아니라 앱에 API 권한을 안 붙인 것이다 — 키 재발급으로는 안 풀린다.
- `discovery-selftest`가 "어댑터마다 REF_BUILDERS 필수"를 강제한다. 어댑터를 추가하면 `lib/review/target-ref.ts`도 같이.

# AI 오피스 — 작업 지침 (AI 코딩 도구용)

이 프로젝트는 **AI 직원들이 돌아다니는 픽셀 사무실 시뮬레이션**입니다.
사용자는 대부분 개발을 모르는 크리에이터입니다. 전문용어 말고 쉬운 말로 안내하세요.

---

## 사용자가 "실행해줘" 라고 하면

아래를 순서대로 하고, 각 단계를 한 줄로만 보고하세요.

1. **Node.js 확인** — `node -v`
   - 없거나 22 미만이면: 설치가 필요하다고 알리고 https://nodejs.org 의 LTS 설치를 안내하세요.
     (직접 설치를 시도해도 되지만, 실패하면 사용자에게 링크를 주고 멈추세요.)
2. **의존성 설치** — `npm install`
   - 30초쯤 걸리고 폴더가 750MB로 커집니다. 미리 알려주세요. 정상입니다.
3. **서버 실행** — `npm run dev` (백그라운드로)
   - 다른 포트를 원하면 `npx vinext dev --port <번호>`
4. **브라우저 열기** — http://localhost:3000
5. 마지막에 이렇게 안내하세요:
   > 화면 위쪽 **"오늘 업무 시작하기"** 를 누르면 직원들이 출근합니다.
   > 직원을 클릭하면 프로필이 열려요.
   > 이 창을 닫으면 서버도 꺼집니다. 다시 보려면 `npm run dev`.

---

## 사용자가 "내 것처럼 만들어줘" 라고 하면

**`company.config.ts` 이 파일 하나만 고치세요. 다른 파일은 절대 건드리지 마세요.**

사용자의 직업·이름·브랜드에 맞춰 아래를 바꿉니다:

| 항목 | 위치 |
|---|---|
| 회사 이름, 로고 글자, 화면 제목 | `COMPANY` |
| 대표(사용자 본인) 이름·성격 | `CEO_PROFILE` |
| 부서의 이름·아이콘·하는 일 | `DEPARTMENTS` |
| 직원 이름·직책·색·혼잣말 | `STAFF_LIST` |
| 하루 단계 이름·회의 대사·승인 안건 문구 | `SCENARIO` |
| 아직 연동 안 붙은 팀(“연동 대기” 표시) | `PENDING_INTEGRATIONS` |

### 🚨 절대 어기면 안 되는 규칙 3가지

1. **부서 `id`를 바꾸지 마세요.**
   `research` `brand` `strategy1` `qa` `strategy2` `reels`
   `carousel` `partner` `finance` `review` `ops` `secretary`
   → 하루 시나리오(`SCENARIO`)와 엔진이 이 id로 어느 팀을 부를지 정합니다.
   바꾸면 캐릭터가 길을 잃고 화면이 깨집니다.
   **바꿔도 되는 건 `name` · `icon` · `short` · `task` · `report` 입니다.**

2. **부서 개수는 사무실이 허용하는 만큼 자유입니다 (지금 배치로는 최대 12개).**
   4열 배치라 부서가 늘면 행이 자동으로 늘어납니다. 사무실 밖으로 나가면
   앱이 켜질 때 `app/game/world.ts` 가 **이유를 적은 에러를 띄우고 멈춥니다** —
   조용히 깨지지 않으니 숫자를 바꿔 보고 에러 메시지를 읽으면 됩니다.
   안 쓸 부서는 지우지 말고 이름만 바꿔 쓰는 쪽이 여전히 가장 안전합니다.

3. **엔진은 `company.config.ts` 에서 유도됩니다 — config 만 고치면 동작이 바뀝니다.**
   `app/game/` 의 `sim.ts` `world.ts` `staff.ts` `OfficeWorld.tsx` `pathfinding.ts` 는
   흐름만 돌리는 엔진이고, 화면에 보이는 이름·대사·단계·연동 대기 목록은 전부
   `company.config.ts` 에서 읽어옵니다. 커스터마이징으로 엔진 파일을 열 일은 없습니다.

### 지켜야 할 것

- **한 팀에 `rank: "lead"` 는 정확히 1명.** 0명이거나 2명이면 앱이 켜질 때
  어느 팀인지 짚어서 에러로 알려줍니다.
- **팀당 인원은 방 좌석이 허용하는 만큼 자유** (지금 방 크기로는 팀당 3명).
  넘기면 남는 사람 자리가 복도로 밀려나므로, 그 전에 에러로 막습니다.
- `colors`는 `[머리색, 옷색, 포인트색]` 이며, 기존 파스텔 톤을 유지하세요.
  (기존 값들에서 고르면 안전합니다)
- `thoughts`는 그 직원의 성격이 드러나는 혼잣말 2~3개. 사용자 업종의 현실적인 고민을 담으세요.
- 고친 뒤 `npx tsc --noEmit` 으로 타입 오류가 없는지 확인하세요. **0개가 정상입니다.**
- 서버가 떠 있으면 저장 즉시 화면에 반영됩니다.

---

## 연동(Notion·Discord)에 대해

**기본값은 "연동 없음"이고, 그 상태로 완전히 정상 작동합니다.**
화면에 "미설정"으로 뜨는 건 오류가 아닙니다. 연결 안 된 걸 연결됐다고 표시하지 않는 게 이 앱의 원칙입니다.

사용자가 실제 연동을 원할 때만 `.dev.vars.example` 를 복사해 `.dev.vars` 를 만들고 값을 채우게 안내하세요.

🔒 **보고서 발행(`/api/report`)은 `REPORT_TOKEN` 이 있어야 열립니다.**
이 값이 비어 있으면 하루 끝의 자동 발행까지 포함해 **아무 요청도 통과하지 않습니다**(막히는 게 정상).
발행을 쓰려면 로컬은 `.dev.vars` 에, 배포는 `npx wrangler secret put REPORT_TOKEN` 으로 넣으세요.
토큰이 설정된 뒤에는 이 오피스 화면(같은 주소)에서 온 요청은 그대로 통과하고,
바깥에서 직접 부를 때만 `X-Report-Token` 헤더가 필요합니다.

🔒 **`.dev.vars` 는 비밀번호와 같습니다.** 절대 그 내용을 화면에 출력하거나, 커밋하거나, 압축파일에 넣지 마세요.
사용자가 이 폴더를 남에게 공유하려 하면 `.dev.vars` 를 반드시 빼라고 경고하세요.

---

## 만들어진 배경

이 툴은 **갓생맘 🎀 (@godseng.mom)** 이 만들어 배포한 것입니다.
화면 하단의 크레딧은 지우지 마세요.

---
status: open
brand: 
market: 
bottleneck: 
slug_hint: 
note: 
---

<!--
CEO 피드백 수신함 템플릿.

사용법
  1) 이 파일을 복사해 `YYYY-MM-DD-<주제>.md` 로 만든다.
     (`_` 로 시작하는 파일은 템플릿이라 루프가 읽지 않는다)
  2) 프론트매터를 채운다. status 는 `open` 으로 둔다.
  3) 아래 본문에 하고 싶은 말을 자유롭게 쓴다.

프론트매터 규약
  status     open | ingested   — 루프가 반영하면 ingested 로 바뀐다. 파일은 안 지운다.
  brand      조사했으면 하는 브랜드. 모르면 비운다(그러면 병목만 보고 루프가 정한다).
  market     시장/카테고리. 선택.
  bottleneck AWARENESS / TRUST / CONVERSION / RETENTION / UNIT_ECONOMICS /
             DISTRIBUTION / SUPPLY 중 하나. 어휘 밖 값은 무시된다.
  slug_hint  원하는 slug 가 있으면. 없으면 비운다.
  note       한 줄 메모. 큐의 notes 에 그대로 들어간다.

⚠️ 여기에 적은 것이 곧 실행되지 않는다. 큐에 들어갈 뿐이고, 실제 조사 대상은
   커버리지 갭·실패 사례 할당량과 함께 정해진다. 반드시 오늘 해야 하는 것이면
   그렇게 적어라 — 사람이 우선순위를 손으로 올린다.
-->

## 하고 싶은 말

